#include "main.h"

/**
 * positive_or_negative -  returns if an integer is positive
 * or negative
 * Retrun: Always 0 (Success)
 */

int main(void)
{
	int i;

	i = 98;
	positive_or_negative(i);
	return (0);
}
